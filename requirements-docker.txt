# ============================================================
# requirements-docker.txt — API uniquement, CPU-only
# MODIF 007 — Requirements allégés pour Docker
# Réf : modifs/007_requirements_docker.txt
# ============================================================
# Pas de tensorflow, keras, mediapipe, opencv — inutiles en API
# PyTorch CPU-only = ~800 Mo au lieu de ~3.5 Go avec CUDA
# ============================================================

fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9
python-dotenv==1.0.1
pyyaml>=6.0
click==8.1.8

# NLP — BARThez (PyTorch CPU uniquement)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
transformers>=4.44.0
sentencepiece==0.2.1
tokenizers==0.19.1
protobuf>=3.20.0
accelerate
gTTS==2.5.4
